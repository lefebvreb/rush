/*use wasm_bindgen::prelude::*;

use engine::Engine;

pub struct JsChess(Engine);

impl JsChess {
    pub fn new(fen: &str) -> JsChess {
        //JsChess(Engine::new(fen.unwrap()))
        todo!()
    }
}*/